using System;
using SkredUtils;

public class AbilityShopMenu
{
    private Ref<bool> IsOpen;

    public Ref<bool> Open()
    {
        IsOpen = new Ref<bool>(false);
        throw new NotImplementedException();
    }
}